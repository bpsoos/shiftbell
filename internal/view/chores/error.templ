package chores

import choreviewmodels "github.com/bpsoos/shiftbell/internal/models/view/chores"

templ errorContent(model choreviewmodels.Error) {
	<section aria-labelledby="error-heading">
		<style>
			me { margin-inline: auto; max-width: 42rem; }
			me > .card { background: var(--sb-surface); border-color: var(--sb-danger); }
			me > .card > .card-body { padding: 1.5rem; }
			me [data-actions] { display: flex; flex-wrap: wrap; gap: 0.5rem; }
			me [data-primary-action] { background: var(--sb-accent); border-color: var(--sb-accent); color: var(--sb-accent-text); font-weight: 700; }
			me [data-primary-action]:hover { background: var(--sb-accent-hover); border-color: var(--sb-accent-hover); color: var(--sb-accent-text); }
		</style>
		<div class="card border-danger">
			<div class="card-body">
				<h1 id="error-heading" class="h3">Unable to continue</h1>
				<div class="alert alert-danger" role="alert">{ model.Message }</div>
				if len(model.Links) > 0 {
					<div data-actions>
						for index, link := range model.Links {
							if link.Href != "" {
								<a
									if index == 0 {
										class="btn btn-primary"
										data-primary-action
									} else {
										class="btn btn-outline-secondary"
									}
									href={ link.Href }
									hx-get={ link.Href }
									hx-target="#main"
									hx-swap="outerHTML"
									hx-push-url="true"
								>{ link.Label }</a>
							}
						}
					</div>
				}
			</div>
		</div>
	</section>
}
