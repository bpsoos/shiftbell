package chores

import (
	"time"

	choreapimodels "github.com/bpsoos/shiftbell/internal/models/api/chores"
	choreviewmodels "github.com/bpsoos/shiftbell/internal/models/view/chores"
	viewicons "github.com/bpsoos/shiftbell/internal/view/icons"
)

templ detailContent(t *Templater, model choreviewmodels.Detail, today time.Time) {
	<section aria-labelledby="chore-heading">
		<style>
			me { margin-inline: auto; max-width: 42rem; }
			me > [data-back] { align-items: center; color: var(--sb-muted); display: inline-flex; font-size: 0.875rem; gap: 0.375rem; margin-bottom: 1.125rem; text-decoration: none; }
			me > [data-back]:hover, me > [data-back]:focus-visible { color: var(--sb-text); }
			me [data-notice] { align-items: center; background: var(--sb-surface); border: 1px solid var(--sb-border); border-left: 3px solid var(--sb-accent); color: var(--sb-text); display: flex; font-size: 0.875rem; gap: 0.5rem; margin-bottom: 0.875rem; padding: 0.625rem 0.75rem; }
			me [data-notice] svg { color: var(--sb-muted); flex: 0 0 auto; }
			me > [data-chore-detail] { background: var(--sb-surface); border: 2px solid var(--sb-frame); overflow-wrap: anywhere; padding: 1.5rem 1.25rem 1.25rem; position: relative; }
			me > [data-chore-detail]::before { background: var(--sb-accent); content: ""; height: 3px; left: 0; position: absolute; right: 0; top: 0; }
			me [data-detail-header] { align-items: flex-start; display: flex; gap: 1rem; justify-content: space-between; }
			me h1 { font-size: 1.875rem; letter-spacing: -0.035em; line-height: 1.1; margin: 0; }
			me [data-edit-action] { flex: 0 0 auto; gap: 0.375rem; }
			me [data-deadline] { border-top: 1px solid var(--sb-border); margin-top: 1rem; padding-top: 0.875rem; }
			me [data-due-state] { align-items: center; display: flex; font-size: 0.9375rem; font-weight: 650; gap: 0.375rem; }
			me [data-due-state="overdue"] { color: var(--sb-danger); }
			me [data-due-state="today"] { color: var(--sb-warning); }
			me [data-exact-deadline] { color: var(--sb-muted); display: block; font-size: 0.8125rem; margin-top: 0.125rem; }
			me [data-description] { line-height: 1.55; margin: 1.25rem 0 0; white-space: pre-wrap; }
			me [data-repeating] { border-left: 3px solid var(--sb-accent); color: var(--sb-muted-strong); display: inline-block; font-size: 0.6875rem; font-weight: 700; letter-spacing: 0.08em; margin-top: 1.25rem; padding: 0.125rem 0 0.125rem 0.5rem; text-transform: uppercase; }
			@media sm- {
				me > [data-chore-detail] { padding: 1.25rem 1rem 1rem; }
				me h1 { font-size: 1.625rem; }
				me [data-edit-action] { padding-inline: 0.625rem; }
			}
		</style>
		if model.BackHref != "" {
			<a
				data-back
				href={ model.BackHref }
				hx-get={ model.BackHref }
				hx-target="#main"
				hx-swap="outerHTML"
				hx-push-url="true"
			>
				@viewicons.ArrowLeft()
				<span>Chores</span>
			</a>
		}
		if model.Notice != "" {
			<div data-notice role="status">
				@viewicons.CheckCircle()
				<span>{ model.Notice }</span>
			</div>
		}
		@detailCard(t, model.Chore, model.EditHref, today)
	</section>
}

templ detailCard(t *Templater, chore choreapimodels.Representation, editHref string, today time.Time) {
	<article data-chore-detail>
		<div data-detail-header>
			<h1 id="chore-heading">{ chore.Name }</h1>
			if editHref != "" {
				<a
					class="btn btn-outline-secondary"
					data-edit-action
					href={ editHref }
					hx-get={ editHref }
					hx-target="#main"
					hx-swap="outerHTML"
					hx-push-url="true"
				>
					@viewicons.PencilSimple()
					<span>Edit</span>
				</a>
			}
		</div>
		<div data-deadline>
			<div data-due-state={ t.deadlineState(chore.Deadline, today) }>
				if t.deadlineState(chore.Deadline, today) == "overdue" {
					@viewicons.WarningCircle()
				}
				{ t.relativeDeadline(chore.Deadline, today) }
			</div>
			<time data-exact-deadline datetime={ chore.Deadline }>Due { t.exactDeadline(chore.Deadline) }</time>
		</div>
		if chore.Description != "" {
			<p data-description>{ chore.Description }</p>
		}
		if chore.ScheduleId != nil {
			<span data-repeating>Repeating chore</span>
		}
	</article>
}
